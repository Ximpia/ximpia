# coding: utf-8

# python
import logging

from ximpia.core.models import context_view
from ximpia.core.service import view_tmpl

from $project_name import settings

# Logging
logger = logging.getLogger(__name__)

from service import SiteService

@context_view(__name__)
@view_tmpl(__name__)
def home(request, **args):
	# Instantiage SiteService.home and return result
	site = SiteService(args['ctx'])
	result = site.viewHome()
	return result
